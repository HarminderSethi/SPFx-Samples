/**
 * Re-exports for the SPFx extension package. The actual entry point
 * is registered via `config/config.json` and the manifest file.
 */
export { default as ShareViaTeamsChatCommandSet } from './extensions/shareViaTeamsChat/ShareViaTeamsChatCommandSet';
